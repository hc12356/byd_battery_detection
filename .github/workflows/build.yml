name: Build APK

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install buildozer cython==0.29.36

      - name: Install Android SDK & NDK
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-17-jdk wget unzip
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          
          # 下载并配置 Android Command Line Tools
          cd $HOME
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q commandlinetools-linux-11076708_latest.zip
          mkdir -p $HOME/Android/Sdk/cmdline-tools
          mv cmdline-tools $HOME/Android/Sdk/cmdline-tools/latest
          
          export ANDROID_HOME=$HOME/Android/Sdk
          export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools
          
          # 强制接受所有许可
          yes | sdkmanager --licenses || true
          
          # 安装指定版本的组件 (明确指定 build-tools 34.0.0)
          sdkmanager "platform-tools" "platforms;android-33" "build-tools;34.0.0" "ndk;27.0.12077973"

      - name: Build APK with Buildozer
        run: |
          # 设置环境变量，让 Buildozer 使用我们预装的 SDK
          export BUILDOZER_ALLOW_ROOT=1
          export ANDROID_SDK_ROOT=$HOME/Android/Sdk
          export ANDROID_HOME=$HOME/Android/Sdk
          export PATH=$PATH:$HOME/Android/Sdk/cmdline-tools/latest/bin:$HOME/Android/Sdk/platform-tools:$HOME/Android/Sdk/build-tools/34.0.0
          
          # 创建 buildozer.spec 的本地覆盖配置，防止它自动下载其他版本
          mkdir -p .buildozer/android/platform/
          ln -sf $HOME/Android/Sdk .buildozer/android/platform/android-sdk
          
          # 开始打包
          buildozer -v android debug
        env:
          BUILDOZER_ALLOW_ROOT: 1

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: byd-battery-app
          path: bin/*.apk
